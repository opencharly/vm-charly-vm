# 2026.265.1657 — refactor: drop the moved family skills from vm-charly-vm

## Summary

Remove the skill entities whose owning artifacts live in another repo — they now
live as `skill:` entities in their distro-* owner repos (moved by that repo's
producer PR in this wave) and are generated into the marketplace from there.
Keeping a second copy here is an R3 one-owner violation.

Deleted from `charly.yml`:

- `omarchy-vm-skill:`

This PR also bumps this repo's `charly.yml` schema stamp (`charly migrate`) so
`charly box validate` runs against the current toolchain.

## How tested

```
$ charly box validate
charly box validate: OK — 0 warnings, 0 errors
```

The successor entities are LIVE on the owning repos at their pinned tags; the
companion `opencharly/marketplace` pin PRs make the generator resolve them.

## Rulebook compliance

- **R1** — deletion is the counterpart of a completed relocation.
- **R2** — one coherent wave; this is the activation step.
- **R3** — one owner per skill after this landing.
- **R4** — `charly box validate` is the gate.
- **R5** — deprecated copies deleted (not shimmed) in the same wave.
- **R6** — dedicated checkout, PR-only, no force-push.
- **R7** — `charly box validate` ran on this branch.
- **R8/R9** — no labels/plans/schemas; no binary built here.
- **R10** — config class; the marketplace drift gate proves byte-identity.

## Change classification

Config (skill-entity deletion) → `analysed on a live system`. Proof:
`charly box validate` clean; the deleted nodes are absent from this file.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (analysed on a live system)*
