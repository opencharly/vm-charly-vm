# 2026.246.0514 — docs(skills): document the clone build path + declarative snapshots

## Summary

Document the newly-wired `source.kind: clone` build path (from_vm + from_snapshot) and the declarative `snapshot:` block in the vm-family skills:

- **vm skill**: add the `clone` source kind to the Overview, the clone/snapshot commands (`charly vm clone`, `charly vm snapshot {create,create-consistent,capture-declared,list,delete,revert,promote}`) to the Quick Reference, and a "Build a VM on top of another VM (clone)" workflow section.
- **vms-catalog skill**: add the `clone` branch to the file layout, a "source.kind: clone — build a VM on top of another VM" authoring section, and a "Container ↔ VM switching (layered boxes)" section.

## How tested

```
$ cd vm-charly-vm && git show --stat HEAD
commit d20768c ...
 1 file changed, 125 insertions(+), 2 deletions(-)
```

YAML validated (gopls/yaml: no diagnostics on every edit). The generated marketplace corpus regeneration follows the merge (per the marketplace skill's drift gate: a refs bump ships its regenerated corpus in the same PR).

## Rulebook compliance

- **R1:** N/A — no failure surfaced.
- **R3:** edits land in the owning repo's skill: entities (the single source); no duplicated docs.
- **R4:** N/A.
- **R5:** N/A — additive documentation.
- **R6:** feat/ branch, no force-push, no direct main push.
- **R7/R10:** docs-only change class; the live bed R10 belongs to the plugin-vm clone cutover (PR #18).

## Change classification

- **Change class:** docs (skill corpus sources)
- **Verification gate:** YAML validation + git show --stat; marketplace regeneration after merge
- **Attribution tier:** documentation reviewed

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (documentation reviewed)*