# 2026.243.2152 — docs(skills): tighten the 7 vm skill descriptions to trigger-style

## Summary

Tighten the `description:` frontmatter of all 7 vm-charly-vm skill entities to trigger-style index lines ("Use when X"). The descriptions are the skill index injected into every harness system prompt (progressive disclosure: only name + description + location are always in context). Shorter, trigger-style descriptions shrink the index without touching skill names, bodies, or discoverability — **zero capability reduction**: all 7 skills remain discoverable and loadable, and every trigger signal is preserved (each description keeps its "MUST be invoked before / Use before" phrasing).

Description bytes: ~2,200 → ~2,000.

## How tested

- `python3` YAML parse of the edited `charly.yml`: OK — all 7 skill entities parse, descriptions verified (the changed metadata loads cleanly).
- `git diff` review: only `description:` blocks changed; no structural key (`name`/`family`/`owner`/`triggers`/`content`/`references`/`model`) touched; no body content changed.
- The change is metadata-only prose; no behavior, schema, or build surface affected.

## Rulebook compliance

- **R3** — no duplication: each description is a single canonical index line.
- **R5** — no stale references: every new description preserves the original trigger signal (verified per-skill against the old text).
- No pins, no scripts, no schema changes, no direct push to main.

## Change classification

- **Change class:** metadata-prose (skill `description:` fields in `charly.yml`; zero behavior change)
- **Verification gate:** YAML parse (the changed metadata loads cleanly) + diff review
- **Attribution tier:** `analysed on a live system` — the changed path (the skill-description metadata) was parsed and verified live with retained output; the full R10 gate does not apply to a metadata-prose change with no runtime surface.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*